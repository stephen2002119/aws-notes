# AWS Notes
Use `--profile <name-of-profile>` at the end of AWS CLI command to specify profile. Profiles are in `~/.aws/config`.  
Generate an SSO token that will refresh every 8 hours.
```bash
aws sso login --sso-session stephen
```
```bash
git@github.com:stephen2002119/aws-notes.git
```
